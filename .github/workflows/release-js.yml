name: Release JavaScript

on:
  push:
    tags:
      - "openai-js-v*"
      - "openai-agents-js-v*"
      - "claude-code-js-v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to release (e.g. openai-js-v1.0.8, claude-code-js-v0.2.1)"
        required: true

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Resolve package directory from tag
        id: pkg
        run: |
          tag="${{ github.event.inputs.tag || github.ref_name }}"
          case "$tag" in
            openai-js-v*)         dir="packages/js/openai" ;;
            openai-agents-js-v*)  dir="packages/js/openai-agents" ;;
            claude-code-js-v*)    dir="packages/js/claude-code" ;;
            *)                    echo "Unknown tag prefix: $tag" >&2; exit 1 ;;
          esac
          echo "dir=$dir" >> "$GITHUB_OUTPUT"
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "Releasing $tag from $dir"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"

      - name: Install workspace dependencies
        working-directory: packages/js
        run: npm install

      - name: Publish to npm
        working-directory: ${{ steps.pkg.outputs.dir }}
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_V2 }}
